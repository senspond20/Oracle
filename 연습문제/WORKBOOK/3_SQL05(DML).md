```sql

--1. 과목유형 테이블(TB_CLASS_TYPE)에 아래와 같은 데이터를 입력하세요.

INSERT INTO TB_CLASS_TYPE VALUES('01','전공필수');
INSERT INTO TB_CLASS_TYPE VALUES('02','전공선택');
INSERT INTO TB_CLASS_TYPE VALUES('03','교양필수');
INSERT INTO TB_CLASS_TYPE VALUES('04','교양선택');
INSERT INTO TB_CLASS_TYPE VALUES('05','논문지도');

DELETE TB_CLASS_TYPE;
SELECT * FROM TB_CLASS_TYPE;

--2. 춘 기술대학교 학생들의 정보가 포함되어 있는 학생일반정보 테이블을 만들고자 한다.
-- 아래 내용을 참고하여 적절한 SQL 문을 작성하시오. (서브쿼리 이용)

CREATE TABLE TB_학생일반정보(학번,학생이름,주소) AS (
    SELECT STUDENT_NO, STUDENT_NAME, STUDENT_ADDRESS
    FROM TB_STUDENT
);

SELECT * FROM TB_학생일반정보;

--3. 국어국문학과 학생들의 정보만이 포함되어 있는 학과 정보 테이블을 만들고자 한다.
-- 아래 내용을 참고하여 적절한 SQL문을 작성하시오. (힌트 : 방법은 다양, 소신껏 작성)

CREATE TABLE TB_국어국문학과(학번,학생이름,출생년도,교수이름) AS(
    SELECT STUDENT_NO,STUDENT_NAME, EXTRACT (YEAR FROM TO_DATE(SUBSTR(STUDENT_SSN,1,2),'RR')), PROFESSOR_NAME
    FROM TB_STUDENT
    JOIN TB_PROFESSOR ON( COACH_PROFESSOR_NO = PROFESSOR_NO)
);

DROP TABLE TB_국어국문학과;
SELECT * FROM TB_국어국문학과;
--4. 현 학과들의 정원을 10% 증가시키게 되었다. 이에 사용할 SQL문을 작성(단, 반올림하여 소수점 자릿수는 생기지 않도록)
COMMIT;
SELECT * FROM TB_DEPARTMENT;
--풀이
UPDATE TB_DEPARTMENT
SET CAPACITY = ROUND(CAPACITY * 1.1);

ROLLBACK;
--5. 학번 A413042 인 박건우 학생의 주소가 "서울시 종로구 숭인동 181-2"로 변경되었다고 한다. 주소지를 정정하기 위한 SQL문 작성

-- 풀이
UPDATE TB_STUDENT
SET STUDENT_ADDRESS = '서울시 종로구 숭인동 181-2'
WHERE STUDENT_NO = 'A413042';

ROLLBACK;
-- 확인
SELECT STUDENT_ADDRESS
FROM TB_STUDENT
WHERE STUDENT_NO = 'A413042';


--6. 주민등록번호 보호법에 따라 학생정보 테이블에서 주민번호 뒷자리를 저장하지 않기로 결정; 
-- 이 내용을 반영할 SQL문 작성
-- EX 830530-2124663 -> 830530 
UPDATE TB_STUDENT
SET STUDENT_SSN = SUBSTR(STUDENT_SSN,1,6);

ROLLBACK;
SELECT * FROM TB_STUDENT;

-- 7. 의학과 김명훈 학생은 2005년 1학기 자신이 수강한 '피부생리학' 점수가 잘못되었다는 것을 발견하고 정정을 요청
-- 담당 교수의 확인 받은 결과 해당 과목의 학점을 3.5 로 변경키로 결정. SQL문 작성

--1단계) 
SELECT * FROM TB_STUDENT
    JOIN TB_DEPARTMENT USING(DEPARTMENT_NO)
    JOIN TB_GRADE USING(STUDENT_NO)
    JOIN TB_CLASS USING(CLASS_NO)
WHERE DEPARTMENT_NAME = '의학과' AND STUDENT_NAME = '김명훈' AND
      CLASS_NAME = '피부생리학';

--2) 뷰를 생성해서 수정

CREATE OR REPLACE VIEW TTT
AS (SELECT POINT FROM TB_STUDENT
        JOIN TB_DEPARTMENT USING(DEPARTMENT_NO)
        JOIN TB_GRADE USING(STUDENT_NO)
        JOIN TB_CLASS USING(CLASS_NO)
    WHERE DEPARTMENT_NAME = '의학과' AND STUDENT_NAME = '김명훈' AND
          CLASS_NAME = '피부생리학');
UPDATE TTT SET POINT = 3.5;
      
ROLLBACK;

--
UPDATE /*+ BYPASS_UJVC*/
(
   SELECT * FROM TB_STUDENT
        JOIN TB_DEPARTMENT USING(DEPARTMENT_NO)
        JOIN TB_GRADE USING(STUDENT_NO)
        JOIN TB_CLASS USING(CLASS_NO)
    WHERE DEPARTMENT_NAME = '의학과' AND STUDENT_NAME = '김명훈' AND
          CLASS_NAME = '피부생리학'
)
SET POINT = 3.5;

ROLLBACK;
    

-- 8.성적 테이블(TB_GRADE)에서 휴학생들의 성적항목을 제거.

--1단계)
SELECT * FROM TB_GRADE
JOIN TB_STUDENT USING(STUDENT_NO)
WHERE ABSENCE_YN = 'Y';

UPDATE /*+ BYPASS_UJVC */
(
    SELECT * FROM TB_GRADE
    JOIN TB_STUDENT USING(STUDENT_NO)
    WHERE ABSENCE_YN = 'Y'
)
SET POINT = 0;

-- 풀이
DELETE FROM TB_GRADE
WHERE STUDENT_NO IN(SELECT STUDENT_NO FROM TB_GRADE
    JOIN TB_STUDENT USING(STUDENT_NO)
    WHERE ABSENCE_YN = 'Y'
);

SELECT * FROM TB_GRADE;
ROLLBACK;


```